
// import {useQuery,useMutation,useQueryClient} from '@tanstack/react-query';
// import { isLicence } from '../../auth/api/fetchData';

//     const queryClient = useQueryClient();
//    export const useLicenseQuery = useMutation(isLicence, {
//         onSuccess: (data) => {
//           queryClient.invalidateQueries([``]);
//           data?setNol(false):setNol(true)
//           data?/*Toast.show({
//             type: 'success',
//             text1: 'correct License',
//             //text2: `this inputs are required ${fullname?"":"full"} ${email?"":"email"}, ${phoneNumber?"":"phone"}, ${password?"":"password"}`
//           })*/null:Toast.show({
//             type: 'error',
//             text1: 'Wrong License',
//             //text2: `this inputs are required ${fullname?"":"full"} ${email?"":"email"}, ${phoneNumber?"":"phone"}, ${password?"":"password"}`
//           });
//         },
//       });
